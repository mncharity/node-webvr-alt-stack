{expect} = require('chai')
events = require('../src/events')

describe 'emit_gamepadconnected', ->
  before ->
    @skip()

  it 'has a test'

describe 'emit_gamepaddisconnected', ->
  before ->
    @skip()

  it 'has a test'

describe 'GamepadEvent mock', ->
  before ->
    @skip()

  it 'has a test'
